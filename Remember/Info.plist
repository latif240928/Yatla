<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- ====================================================== -->
	<!--   Backend wiring                                         -->
	<!-- ====================================================== -->
	<!-- Allowed values: mock | local | development | staging | production -->
	<key>API_ENV</key>
	<string>local</string>

	<!--
		Optional override that wins over `API_ENV.baseURL`. Useful when
		the backend dev shares a LAN IP or a temporary tunnel URL — set
		this to e.g. "http://192.168.1.42:8000/api/v1" without touching
		the source. Leave empty to fall back to `API_ENV.baseURL`.
	-->
	<key>API_BASE_URL</key>
	<string></string>

	<!-- ====================================================== -->
	<!--   App Transport Security                                 -->
	<!-- Allows plain HTTP to localhost / private LAN IPs so the   -->
	<!-- simulator can reach a local FastAPI/uvicorn backend.      -->
	<!-- Production endpoints are still required to be HTTPS.      -->
	<!-- ====================================================== -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
		<key>NSExceptionDomains</key>
		<dict>
			<key>localhost</key>
			<dict>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
				<key>NSIncludesSubdomains</key>
				<true/>
			</dict>
		</dict>
	</dict>

	<!-- ====================================================== -->
	<!--   Standard iOS app boilerplate                           -->
	<!--   (Xcode otherwise auto-generates these from build       -->
	<!--    settings; we now manage them explicitly.)             -->
	<!-- ====================================================== -->
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>

	<key>UILaunchScreen</key>
	<dict/>

	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<true/>
		<key>UISceneConfigurations</key>
		<dict/>
	</dict>

	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>

	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>

	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
